//main method
wdym non main str[] args :)
    //user inputs a number 
    xd "type a number to see if it is prime!"
    //variables
    num aka hmu
    flag aka false
    //the shit
    lmfao i aka 2 ? i < Math.Sqrt(num) ? ily i :)
        lmao num % i is 0 :)
            flag aka true
            break

    lmao !flag :)
        xd num " is a prime number"
    afk :)
        xd num " is not a prime number"

